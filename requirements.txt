fastapi==0.115.0
uvicorn[standard]==0.30.6
# 0.34.2 predated GA prompt caching: cache_control was not a typed field and
# caching lived behind client.beta.prompt_caching, so whether the cache_control
# blocks in services/agent.py did anything was a guess. on this version it is
# first class on messages.create and Usage declares the cache counters
anthropic==0.122.0
twilio==9.3.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
google-api-python-client==2.147.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
httplib2==0.22.0
redis==5.1.1
python-multipart==0.0.12
httpx==0.27.2
cryptography==43.0.1

# no sentry package on purpose. error monitoring is the counters and the
# heartbeat in services/monitoring.py, which need nothing installed and no
# account. if you ever want stack traces and alerting instead of reading logs,
# pip install sentry-sdk and set SENTRY_DSN, and the hook there picks it up
